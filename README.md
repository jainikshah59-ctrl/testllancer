# Collancer

Clean production package for Vercel deployment.

- No node_modules included.
- Hero video uses a lightweight poster and metadata preload.
- Run `npm ci` then `npm run build` before deployment.


## V33 background update
The hero keeps its video background. All remaining sections now share a lightweight, CSS-animated ambient background inspired by the hero palette, replacing the previous canvas/section background treatment.
